{% extends 'layout/main.html.twig' %}

{% block stylesheets %}
    <link rel="stylesheet" href="{{ asset('plugins/toastr/toastr.min.css') }}">
    <link rel="stylesheet" href="{{ asset('plugins/json-viewer/json-viewer.min.css') }}">
{% endblock %}
{% block content %}
    {% apply spaceless %}
        <div class="row">
            <div class="{% if is_auditable(data) and can_view_audit() %}col-lg-6{% else %}col-lg-12{% endif %} col-md-12 col-sm-12">
                <div class="card">
                    <div class="card-header">
                        <h4>{{ page_title | trans({}, 'pages') }}</h4>
                    </div>
                    <div class="card-body p-1">
                        <ul class="list-group">
                            {% for property in properties %}
                                {% if property.name not in ["id", "startAt", "lastPing", "uptime", "lastDown", "downtime", "status"] %}
                                    <li class="list-group-item text-primary">
                                        {{ ('sas.table.column.' ~ context ~ '.' ~ property.name) | trans({}, 'tables') }}
                                        <a class="float-right">{{ semart_print(attribute(data, property.name)) }}</a>
                                    </li>
                                {% endif %}
                            {% endfor %}
                            {% set info = node_info(data) %}
                            {% if info is not empty %}
                                <li class="list-group-item text-primary">
                                    {{ ('sas.table.column.' ~ context ~ '.license_name') | trans({}, 'tables') }}
                                    <a class="float-right">{{ info["name"] }}</a>
                                </li>
                                <li class="list-group-item text-primary">
                                    {{ ('sas.table.column.' ~ context ~ '.license_email') | trans({}, 'tables') }}
                                    <a class="float-right">{{ info["email"] }}</a>
                                </li>
                                <li class="list-group-item text-primary">
                                    {{ ('sas.table.column.' ~ context ~ '.license_company') | trans({}, 'tables') }}
                                    <a class="float-right">{{ info["company"] }}</a>
                                </li>
                                <li class="list-group-item text-primary">
                                    {{ ('sas.table.column.' ~ context ~ '.license_issued_by') | trans({}, 'tables') }}
                                    <a class="float-right">{{ info["issued_by"] }}</a>
                                </li>
                                <li class="list-group-item text-primary">
                                    {{ ('sas.table.column.' ~ context ~ '.license_valid_until') | trans({}, 'tables') }}
                                    <a class="float-right">{{ info["valid_until"] | date('Y-m-d H:i:s', 'Asia/Jakarta') }}</a>
                                </li>
                                <li class="list-group-item text-primary">
                                    {{ ('sas.table.column.' ~ context ~ '.license_last_used') | trans({}, 'tables') }}
                                    <a class="float-right">{{ info["activate_at"] | date('Y-m-d H:i:s', 'Asia/Jakarta') }}</a>
                                </li>
                                <li class="list-group-item text-primary">
                                    {{ ('sas.table.column.' ~ context ~ '.license_max_node') | trans({}, 'tables') }}
                                    <a class="float-right">{{ info["max_node"] }}</a>
                                </li>
                                <li class="list-group-item text-primary">
                                    {{ ('sas.table.column.' ~ context ~ '.license_max_service') | trans({}, 'tables') }}
                                    <a class="float-right">{{ info["max_service"] }}</a>
                                </li>
                            {% endif %}
                        </ul>
                    </div>
                    <div class="card-footer">
                        <a href="{{ path('KejawenLab\\Application\\Admin\\Controller\\Node\\Main') }}" class="btn btn-outline-secondary">{{ 'sas.form.action.back' | trans({}, 'forms') }}</a>
                        <a href="{{ path('KejawenLab\\Application\\Admin\\Controller\\Endpoint\\Main', {'nodeId': data.id }) }}" class="btn btn-outline-primary ml-1">{{ 'sas.form.action.view_endpoint' | trans({}, 'forms') }}</a>
                        <a href="{{ path('KejawenLab\\Application\\Admin\\Controller\\Node\\Ping', {'id': data.id }) }}" class="btn btn-outline-danger ml-1">{{ 'sas.form.action.ping' | trans({}, 'forms') }}</a>
                    </div>
                </div>
            </div>
            {% if is_auditable(data) and can_view_audit() %}
            <div class="col-lg-6 col-md-12 col-sm-12">
                <div class="card">
                    <div class="card-header p-2">
                        <h4>{{ 'sas.page.audit.last_changes' | trans({}, 'pages') }}</h4>
                    </div>
                    <div class="card-body p-0">
                        {% for key, data in audits %}
                            <ul class="list-group p-1">
                                {% for property in data | keys %}
                                    <li class="list-group-item text-primary">
                                        {{ ('sas.table.column.audit.' ~ property) | trans({}, 'tables') }}
                                        {% if 'data' == property %}
                                            <a class="float-right"><pre class="json-viewer" data-json="{{ data[property] | json_encode() }}"></pre></a>
                                        {% else %}
                                            <a class="float-right">{{ data[property] }}</a>
                                        {% endif %}
                                    </li>
                                {% endfor %}
                            </ul>
                        {% endfor %}
                    </div>
                    <div class="card-footer">
                        <a href="{{ path('KejawenLab\\Application\\Admin\\Controller\\Node\\Audit', {id: data.id}) }}" class="btn btn-outline-info">{{ 'sas.form.action.audit' | trans({}, 'forms') }}</a>
                    </div>
                </div>
            </div>
            {% endif %}
        </div>
        <div class="row">
            <div class="col-lg-12 col-md-12 col-sm-12">
                <div class="card">
                    <div class="card-header p-2">
                        <h4>{{ 'sas.page.node.services' | trans({}, 'pages') }}</h4>
                    </div>
                    <div class="card-body p-1">
                        <div class="row">
                            {% set services = node_services(data) %}
                            {% for service in services %}
                                <div class="col-lg-6 col-md-12 col-sm-12">
                                    <div class="card mt-1">
                                        <div class="card-body p-0">
                                            <ul class="list-group p-0">
                                                <li class="list-group-item text-primary">
                                                    {{ 'sas.table.column.node.service_type' | trans({}, 'tables')}}
                                                    <a class="float-right">{{ service.type | upper }}</a>
                                                </li>
                                                <li class="list-group-item text-primary">
                                                    {{ 'sas.table.column.node.service_port' | trans({}, 'tables')}}
                                                    <a class="float-right">{{ service.port }}</a>
                                                </li>
                                                <li class="list-group-item text-primary">
                                                    {{ 'sas.table.column.node.service_name' | trans({}, 'tables')}}
                                                    <a class="float-right">{{ service.name }}</a>
                                                </li>
                                                <li class="list-group-item text-primary">
                                                    {{ 'sas.table.column.node.service_uptime' | trans({}, 'tables')}}
                                                    <a class="float-right">{{ service.uptime }}</a>
                                                </li>
                                                <li class="list-group-item text-primary">
                                                    {{ 'sas.table.column.node.service_successed' | trans({}, 'tables')}}
                                                    <a class="float-right">{{ service.successed }}</a>
                                                </li>
                                                <li class="list-group-item text-primary">
                                                    {{ 'sas.table.column.node.service_failed' | trans({}, 'tables')}}
                                                    <a class="float-right">{{ service.failed }}</a>
                                                </li>
                                                <li class="list-group-item text-primary">
                                                    {{ 'sas.table.column.node.service_description' | trans({}, 'tables')}}
                                                    <a class="float-right">{{ service.description }}</a>
                                                </li>
                                            </ul>
                                        </div>
                                    </div>
                                </div>
                            {% endfor %}
                        </div>
                    </div>
                </div>
            </div>
        </div>
    {% endapply %}
{% endblock %}
{% block javascripts %}
    <script src="{{ asset('plugins/toastr/toastr.min.js') }}"></script>
    <script src="{{ asset('plugins/json-viewer/json-viewer.min.js') }}"></script>
    <script>
        $(document).ready(function () {
            $('.json-viewer').each(function() {
                let me = $(this);
                me.jsonViewer(me.data('json'), {collapsed: true, withQuotes: true, withLinks: false});
            });

            {% for message in app.flashes('info') %}
            toastr['success']('{{ message | trans({}, 'pages') }}');
            {% endfor %}
            {% for message in app.flashes('error') %}
            toastr['error']('{{ message | trans({}, 'pages') }}');
            {% endfor %}
        });
    </script>
{% endblock %}
